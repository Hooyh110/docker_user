{
  "scripts": {
    "dev": "yarn install && vuepress dev",
    "build": "yarn install && vuepress build"
  },
  "devDependencies": {
    "@vuepress/plugin-back-to-top": "^1.0.0-alpha.42",
    "@vuepress/plugin-pwa": "^1.0.0-alpha.42",
    "moment": "^2.24.0",
    "vuepress": "^1.0.0-alpha.42"
  }
}
